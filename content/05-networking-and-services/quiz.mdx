import Checkpoint from "@/components/quiz/Checkpoint";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-accent uppercase tracking-wide mb-1">quiz</p>
  <h1 className="text-2xl font-bold">Networking and Services</h1>
  <p className="text-fg-subtle text-sm mt-2">8 questions covering all lessons in this module.</p>
</header>

<Checkpoint id="m05-q1"
  question={<>In the Kubernetes network model, Pods can reach other Pods in the cluster:</>}
  choices={[
    { id: "a", label: "Only through a Service" },
    { id: "b", label: "Directly by IP, without NAT between Pods" },
    { id: "c", label: "Only if they're in the same namespace" },
    { id: "d", label: "Only if they're on the same node" },
  ]}
  correctId="b"
  feedback={<p>The K8s networking model guarantees flat, NAT-free Pod-to-Pod connectivity. This is implemented by a CNI plugin. Services exist for stability (Pods are ephemeral) but aren&apos;t required for Pod-to-Pod traffic.</p>}
/>

<Checkpoint id="m05-q2"
  question={<>A Service needs to be reachable only from within the cluster. Which type?</>}
  choices={[
    { id: "a", label: "ClusterIP" },
    { id: "b", label: "NodePort" },
    { id: "c", label: "LoadBalancer" },
    { id: "d", label: "ExternalName" },
  ]}
  correctId="a"
  feedback={<p>ClusterIP (the default) is internal-only. NodePort and LoadBalancer expose outside the cluster. ExternalName is a DNS alias to something outside the cluster.</p>}
/>

<Checkpoint id="m05-q3"
  question={<>What's the role of an Ingress controller?</>}
  choices={[
    { id: "a", label: "It's a virtual Pod that acts like a Service" },
    { id: "b", label: "A running workload (NGINX, Traefik, etc.) that reads Ingress objects and actually routes HTTP traffic" },
    { id: "c", label: "A managed cloud load balancer" },
    { id: "d", label: "Built into every Kubernetes cluster" },
  ]}
  correctId="b"
  feedback={<p>Ingress objects are just configuration. An Ingress controller is a running workload that implements the routing rules. Without one installed, Ingress does nothing.</p>}
/>

<Checkpoint id="m05-q4"
  question={<>What's the fully-qualified DNS name of a Service <code>billing</code> in namespace <code>core</code>?</>}
  choices={[
    { id: "a", label: "billing" },
    { id: "b", label: "billing.core" },
    { id: "c", label: "billing.core.svc.cluster.local" },
    { id: "d", label: "core.billing.svc.cluster.local" },
  ]}
  correctId="c"
  feedback={<p>Format is <code>&lt;service&gt;.&lt;namespace&gt;.svc.&lt;cluster-domain&gt;</code>. The default cluster domain is <code>cluster.local</code>. Short forms like <code>billing</code> (same ns) and <code>billing.core</code> (different ns) also work thanks to DNS search suffixes.</p>}
/>

<Checkpoint id="m05-q5"
  question={<>Which component provides service discovery DNS in modern Kubernetes clusters?</>}
  choices={[
    { id: "a", label: "kube-dns (legacy)" },
    { id: "b", label: "CoreDNS" },
    { id: "c", label: "kube-proxy" },
    { id: "d", label: "etcd" },
  ]}
  correctId="b"
  feedback={<p>CoreDNS replaced kube-dns as the default. It&apos;s a CNCF Graduated project - pluggable, lightweight, written in Go.</p>}
/>

<Checkpoint id="m05-q6"
  question={<>A <code>NetworkPolicy</code> is applied to Pods labeled <code>app=db</code>, allowing ingress only from <code>app=api</code>. What happens to a request from an unrelated Pod?</>}
  choices={[
    { id: "a", label: "It's allowed (no explicit deny)" },
    { id: "b", label: "It's blocked (applying a NetworkPolicy flips the selected Pods to default-deny)" },
    { id: "c", label: "It's logged but allowed" },
    { id: "d", label: "It succeeds only if the CNI is Calico" },
  ]}
  correctId="b"
  feedback={<p>When a NetworkPolicy selects a Pod, that Pod becomes default-deny for ingress (or egress, if policy covers it). Only traffic explicitly allowed by the policy gets through. CNI enforcement is required.</p>}
/>

<Checkpoint id="m05-q7"
  question={<>What does a service mesh typically add to a cluster?</>}
  choices={[
    { id: "a", label: "A built-in Kubernetes API server replacement" },
    { id: "b", label: "A sidecar proxy in each Pod that handles mTLS, retries, traffic management, and observability" },
    { id: "c", label: "Faster Pod startup times" },
    { id: "d", label: "Automatic CI/CD pipelines" },
  ]}
  correctId="b"
  feedback={<p>Service meshes inject a sidecar proxy (often Envoy) into each Pod. The proxy intercepts network traffic and applies mesh policies without the application needing to know. Istio and Linkerd are prominent meshes.</p>}
/>

<Checkpoint id="m05-q8"
  question={<>Which CNCF Graduated project is widely used as the sidecar proxy in service meshes?</>}
  choices={[
    { id: "a", label: "HAProxy" },
    { id: "b", label: "NGINX" },
    { id: "c", label: "Envoy" },
    { id: "d", label: "Traefik" },
  ]}
  correctId="c"
  feedback={<p>Envoy is a high-performance proxy originally from Lyft, now CNCF Graduated. It&apos;s the sidecar in Istio and in many other mesh and API-gateway solutions.</p>}
/>

</div>
