import Checkpoint from "@/components/quiz/Checkpoint";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-accent uppercase tracking-wide mb-1">quiz</p>
  <h1 className="text-2xl font-bold">Working with kubectl</h1>
  <p className="text-fg-subtle text-sm mt-2">8 questions covering all lessons in this module.</p>
</header>

<Checkpoint
  id="m03-q1"
  question={<>Where does <code>kubectl</code> read cluster connection info from by default?</>}
  choices={[
    { id: "a", label: "/etc/kubernetes/admin.conf" },
    { id: "b", label: "~/.kube/config (or $KUBECONFIG)" },
    { id: "c", label: "A Kubernetes ConfigMap in the cluster" },
    { id: "d", label: "It prompts for credentials each time" },
  ]}
  correctId="b"
  feedback={<p>kubectl reads <code>~/.kube/config</code> by default. The <code>$KUBECONFIG</code> env var can point to an alternate path or multiple paths.</p>}
/>

<Checkpoint
  id="m03-q2"
  question={<>A kubeconfig "context" is:</>}
  choices={[
    { id: "a", label: "The current working directory" },
    { id: "b", label: "A named triple of cluster + user + default namespace" },
    { id: "c", label: "A Kubernetes Namespace" },
    { id: "d", label: "A shell alias for kubectl" },
  ]}
  correctId="b"
  feedback={<p>A context bundles which cluster to talk to, which user to authenticate as, and which namespace to default to. Switch with <code>kubectl config use-context</code>.</p>}
/>

<Checkpoint
  id="m03-q3"
  question={<>Which command shows events and recent state changes for a Pod?</>}
  choices={[
    { id: "a", label: "kubectl logs <pod>" },
    { id: "b", label: "kubectl describe pod <pod>" },
    { id: "c", label: "kubectl get pod <pod> -o json" },
    { id: "d", label: "kubectl exec <pod> -- ps" },
  ]}
  correctId="b"
  feedback={<p><code>kubectl describe</code> shows the object&apos;s full state plus recent Events (scheduling, image pulls, probe failures). <code>logs</code> shows app output. <code>get -o json</code> shows the object spec/status but no events.</p>}
/>

<Checkpoint
  id="m03-q4"
  question={<>You want to see logs from the <em>previous</em> instance of a crashed container. What flag?</>}
  choices={[
    { id: "a", label: "--old" },
    { id: "b", label: "--history" },
    { id: "c", label: "--previous (or -p)" },
    { id: "d", label: "--crashed" },
  ]}
  correctId="c"
  feedback={<p><code>kubectl logs &lt;pod&gt; --previous</code> (or <code>-p</code>) fetches logs from the last terminated instance. Essential for diagnosing crash loops.</p>}
/>

<Checkpoint
  id="m03-q5"
  question={<>What's the difference between <code>kubectl create -f file.yaml</code> and <code>kubectl apply -f file.yaml</code>?</>}
  choices={[
    { id: "a", label: "No difference - they're aliases" },
    { id: "b", label: "create errors if the object exists; apply is idempotent and updates existing objects to match the file" },
    { id: "c", label: "create is for Pods only; apply is for all objects" },
    { id: "d", label: "apply is deprecated and shouldn't be used" },
  ]}
  correctId="b"
  feedback={<p><code>create</code> is imperative and errors on "already exists". <code>apply</code> is declarative: diffs the file against the live object, creates or updates as needed. <code>apply</code> is the standard for real workflows.</p>}
/>

<Checkpoint
  id="m03-q6"
  question={<>Which flag lists resources across all namespaces?</>}
  choices={[
    { id: "a", label: "--global" },
    { id: "b", label: "-A or --all-namespaces" },
    { id: "c", label: "-n all" },
    { id: "d", label: "kubectl list-all" },
  ]}
  correctId="b"
  feedback={<p><code>-A</code> is the shorthand for <code>--all-namespaces</code>. Extremely useful for cluster-wide diagnosis.</p>}
/>

<Checkpoint
  id="m03-q7"
  question={<>How do you generate a YAML manifest from an imperative command <em>without</em> actually creating the object?</>}
  choices={[
    { id: "a", label: "kubectl create deployment nginx --image=nginx --dry-run=client -o yaml" },
    { id: "b", label: "kubectl preview deployment nginx" },
    { id: "c", label: "kubectl create deployment nginx --fake" },
    { id: "d", label: "kubectl yaml nginx" },
  ]}
  correctId="a"
  feedback={<p><code>--dry-run=client -o yaml</code> is the canonical way to scaffold a manifest from an imperative command. Redirect to a file and edit.</p>}
/>

<Checkpoint
  id="m03-q8"
  question={<>You want to filter <code>kubectl get pods</code> to only those with label <code>app=nginx</code>. What flag?</>}
  choices={[
    { id: "a", label: "--filter=app=nginx" },
    { id: "b", label: "--label app=nginx" },
    { id: "c", label: "-l app=nginx" },
    { id: "d", label: "--match=nginx" },
  ]}
  correctId="c"
  feedback={<p><code>-l</code> (or <code>--selector</code>) takes a label selector. You can combine with commas: <code>-l app=nginx,env=prod</code>.</p>}
/>

</div>
