import DebugPuzzle from "@/components/puzzle/DebugPuzzle";
import TerminalBlock from "@/components/TerminalBlock";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-warning uppercase tracking-wide mb-1">debug challenge</p>
  <h1 className="text-2xl font-bold">Working with kubectl</h1>
  <p className="text-fg-subtle text-sm mt-2">
    3 challenges covering contexts, commands, and output interpretation.
  </p>
</header>

<DebugPuzzle
  id="m03-d1"
  syllabusIds={["K8S-F-07"]}
  prompt={<p>You run <code>kubectl get pods</code> and see nothing, but you know workloads are running. What's the first thing to check?</p>}
  artifact={
    <TerminalBlock command="kubectl get pods">
      {"No resources found in default namespace.\n"}
    </TerminalBlock>
  }
  choices={[
    { id: "a", label: "The cluster is probably offline - restart it." },
    { id: "b", label: "kubectl is looking in the default namespace. Try -n <ns> or -A to see other namespaces." },
    { id: "c", label: "Pods are hidden unless you pass --show-all." },
    { id: "d", label: "You need to install a different kubectl version." },
  ]}
  correctId="b"
  explanation={<p><code>kubectl get pods</code> shows only the current context&apos;s namespace (usually <code>default</code>). Use <code>-n core</code> to target a specific namespace, or <code>-A</code> / <code>--all-namespaces</code> to see everything.</p>}
/>

<DebugPuzzle
  id="m03-d2"
  syllabusIds={["K8S-F-07"]}
  prompt={<p>A Pod is in <code>CrashLoopBackOff</code>. You run <code>kubectl logs &lt;pod&gt;</code> and the output is empty. Why?</p>}
  artifact={
    <TerminalBlock command="kubectl logs analytics-ingest-7b9d8">
      {"\n"}
    </TerminalBlock>
  }
  choices={[
    { id: "a", label: "The container is currently being restarted; its fresh instance hasn't logged yet. Use --previous to see the last crashed instance's logs." },
    { id: "b", label: "CrashLoopBackOff Pods don't produce logs at all." },
    { id: "c", label: "You need sudo to read logs." },
    { id: "d", label: "Logs are only available via kubectl describe." },
  ]}
  correctId="a"
  explanation={<p><code>kubectl logs</code> attaches to the current container. On a crash loop the current container is briefly alive and may not have logged yet. <code>kubectl logs &lt;pod&gt; --previous</code> (or <code>-p</code>) shows the logs from the last terminated instance, which is usually what you want.</p>}
/>

<DebugPuzzle
  id="m03-d3"
  syllabusIds={["K8S-F-07"]}
  prompt={<p>You applied a YAML manifest. The object already existed. Which command was idempotent?</p>}
  artifact={null}
  choices={[
    { id: "a", label: "kubectl create -f deployment.yaml" },
    { id: "b", label: "kubectl apply -f deployment.yaml" },
    { id: "c", label: "kubectl run deployment.yaml" },
    { id: "d", label: "kubectl edit deployment.yaml" },
  ]}
  correctId="b"
  explanation={<p><code>kubectl apply</code> creates the object if it doesn&apos;t exist and updates it if it does - idempotent, diff-based. <code>kubectl create</code> errors if the object already exists. <code>kubectl run</code> is a Pod-creation shortcut, not a YAML applier. <code>kubectl edit</code> opens an editor - interactive, not a file-based operation.</p>}
/>

</div>
