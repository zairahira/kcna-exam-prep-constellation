import Checkpoint from "@/components/quiz/Checkpoint";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-accent uppercase tracking-wide mb-1">quiz</p>
  <h1 className="text-2xl font-bold">Kubernetes Security</h1>
  <p className="text-fg-subtle text-sm mt-2">8 questions covering all lessons in this module.</p>
</header>

<Checkpoint id="m08-q1"
  question={<>Which pair grants namespaced permissions?</>}
  choices={[
    { id: "a", label: "ClusterRole + RoleBinding" },
    { id: "b", label: "Role + RoleBinding" },
    { id: "c", label: "Role + ClusterRoleBinding" },
    { id: "d", label: "ClusterRole + ClusterRoleBinding" },
  ]}
  correctId="b"
  feedback={<p>A Role is namespaced. A RoleBinding is namespaced. Together they grant permissions within one namespace. You can also RoleBinding a ClusterRole to scope its rules to one namespace - a common pattern - but the "all-namespaced" pairing is Role + RoleBinding.</p>}
/>

<Checkpoint id="m08-q2"
  question={<>What's the fastest way to check whether a subject can do something?</>}
  choices={[
    { id: "a", label: "Read the kubeconfig" },
    { id: "b", label: "kubectl auth can-i <verb> <resource> --as=<subject>" },
    { id: "c", label: "kubectl describe role <role>" },
    { id: "d", label: "kubectl get clusterrolebindings --all" },
  ]}
  correctId="b"
  feedback={<p><code>kubectl auth can-i</code> is the built-in permission checker. Works for users and ServiceAccounts (<code>--as=system:serviceaccount:&lt;ns&gt;:&lt;sa&gt;</code>).</p>}
/>

<Checkpoint id="m08-q3"
  question={<>A Pod created with no <code>serviceAccountName</code> set uses:</>}
  choices={[
    { id: "a", label: "No ServiceAccount at all" },
    { id: "b", label: "The 'default' ServiceAccount of its namespace" },
    { id: "c", label: "The cluster-admin ServiceAccount" },
    { id: "d", label: "A random ServiceAccount" },
  ]}
  correctId="b"
  feedback={<p>Every namespace has a <code>default</code> ServiceAccount. Pods inherit it unless they specify otherwise. By default it has no permissions.</p>}
/>

<Checkpoint id="m08-q4"
  question={<>What replaced PodSecurityPolicy (PSP)?</>}
  choices={[
    { id: "a", label: "Pod Security Admission (PSA) with Pod Security Standards" },
    { id: "b", label: "Nothing - it's still the default" },
    { id: "c", label: "Kyverno" },
    { id: "d", label: "Dockershim" },
  ]}
  correctId="a"
  feedback={<p>PSP was deprecated in v1.21 and removed in v1.25. The built-in replacement is Pod Security Admission, which enforces the three Pod Security Standards levels via namespace labels.</p>}
/>

<Checkpoint id="m08-q5"
  question={<>Which namespace label enforces the <code>restricted</code> Pod Security level?</>}
  choices={[
    { id: "a", label: "pod-security.kubernetes.io/enforce=restricted" },
    { id: "b", label: "security/restricted=true" },
    { id: "c", label: "k8s.io/pss=restricted" },
    { id: "d", label: "restricted=enforce" },
  ]}
  correctId="a"
  feedback={<p>The format is <code>pod-security.kubernetes.io/&lt;mode&gt;=&lt;level&gt;</code>, where mode is <code>enforce</code>/<code>audit</code>/<code>warn</code> and level is <code>privileged</code>/<code>baseline</code>/<code>restricted</code>.</p>}
/>

<Checkpoint id="m08-q6"
  question={<>A Pod with <code>runAsNonRoot: true</code>:</>}
  choices={[
    { id: "a", label: "Will automatically run as UID 1000" },
    { id: "b", label: "Will be rejected at runtime if the container image's default user is root (UID 0)" },
    { id: "c", label: "Cannot mount any volumes" },
    { id: "d", label: "Is always scheduled to control plane nodes" },
  ]}
  correctId="b"
  feedback={<p><code>runAsNonRoot</code> causes kubelet to refuse to start the container if it would run as UID 0. The image itself must declare a non-root default user (USER directive), or the Pod must specify <code>runAsUser</code>.</p>}
/>

<Checkpoint id="m08-q7"
  question={<>What's the difference between mutating and validating admission controllers?</>}
  choices={[
    { id: "a", label: "Mutating modifies the request; validating accepts or rejects it" },
    { id: "b", label: "Mutating runs after validating" },
    { id: "c", label: "Validating is deprecated" },
    { id: "d", label: "They're the same thing with different names" },
  ]}
  correctId="a"
  feedback={<p>Mutating admission runs first - it can inject sidecars, set defaults, etc. Validating runs after - it accepts or rejects based on rules. Both are webhook-extensible.</p>}
/>

<Checkpoint id="m08-q8"
  question={<>Cosign (part of Sigstore) is used for:</>}
  choices={[
    { id: "a", label: "Scanning images for CVEs" },
    { id: "b", label: "Signing and verifying container images" },
    { id: "c", label: "Generating Dockerfiles" },
    { id: "d", label: "Pushing images to registries" },
  ]}
  correctId="b"
  feedback={<p>Cosign signs images (often using keyless OIDC-backed identities) and verifies signatures. Image scanning (Trivy, Grype) is a separate concern - cosign is about <em>provenance</em>, not <em>vulnerabilities</em>.</p>}
/>

</div>
