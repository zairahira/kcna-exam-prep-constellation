import DebugPuzzle from "@/components/puzzle/DebugPuzzle";
import TerminalBlock from "@/components/TerminalBlock";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-warning uppercase tracking-wide mb-1">debug challenge</p>
  <h1 className="text-2xl font-bold">Pods and Workloads</h1>
  <p className="text-fg-subtle text-sm mt-2">3 challenges covering Pods, Deployments, and lifecycle.</p>
</header>

<DebugPuzzle
  id="m04-d1"
  syllabusIds={["K8S-F-02"]}
  prompt={<p>A Pod owned by a Deployment is deleted with <code>kubectl delete pod &lt;name&gt;</code>. What happens?</p>}
  artifact={null}
  choices={[
    { id: "a", label: "It stays deleted; the Deployment loses a replica." },
    { id: "b", label: "The Deployment's ReplicaSet immediately creates a replacement Pod to maintain desired count." },
    { id: "c", label: "The entire Deployment is rolled back." },
    { id: "d", label: "An error - you can't delete Pods owned by a Deployment." },
  ]}
  correctId="b"
  explanation={<p>The ReplicaSet controller sees actual count dropped below desired and creates a new Pod. This is the core reconcile loop: controllers drive actual state toward desired state continuously.</p>}
/>

<DebugPuzzle
  id="m04-d2"
  syllabusIds={["K8S-F-02"]}
  prompt={<p>A Pod is in <code>CrashLoopBackOff</code> after 5 restart attempts. Which is the most likely root cause to investigate first?</p>}
  artifact={
    <TerminalBlock command="kubectl get pods">
      {"NAME            READY   STATUS             RESTARTS   AGE\n"}
      {"api-7b9d8-abc   0/1     CrashLoopBackOff   5          6m\n"}
    </TerminalBlock>
  }
  choices={[
    { id: "a", label: "The Node is unhealthy - check kubectl get nodes." },
    { id: "b", label: "Kubernetes RBAC is blocking the Pod from starting." },
    { id: "c", label: "The application inside the container is exiting with a non-zero code. Check kubectl logs --previous and recent config changes." },
    { id: "d", label: "The kube-scheduler is misconfigured." },
  ]}
  correctId="c"
  explanation={<p>CrashLoopBackOff means the container keeps exiting. The cause is almost always app-level: bad config, missing dependency, crash on startup. Start with <code>kubectl logs &lt;pod&gt; --previous</code> to see what the last instance said before dying.</p>}
/>

<DebugPuzzle
  id="m04-d3"
  syllabusIds={["K8S-F-02"]}
  prompt={<p>You need to run a Pod on every node, including new nodes added later (e.g., a log shipper). Which workload resource fits?</p>}
  artifact={null}
  choices={[
    { id: "a", label: "Deployment with nodeAffinity" },
    { id: "b", label: "StatefulSet" },
    { id: "c", label: "Job" },
    { id: "d", label: "DaemonSet" },
  ]}
  correctId="d"
  explanation={<p>A <code>DaemonSet</code> ensures exactly one Pod runs on each matching node. When nodes join, the DaemonSet automatically places a Pod on them. When nodes leave, the Pod is removed. This is exactly the "one agent per node" pattern used by log shippers, networking plugins, and monitoring agents.</p>}
/>

</div>
