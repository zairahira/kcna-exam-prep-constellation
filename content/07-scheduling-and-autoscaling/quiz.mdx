import Checkpoint from "@/components/quiz/Checkpoint";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-accent uppercase tracking-wide mb-1">quiz</p>
  <h1 className="text-2xl font-bold">Scheduling and Autoscaling</h1>
  <p className="text-fg-subtle text-sm mt-2">8 questions covering all lessons in this module.</p>
</header>

<Checkpoint id="m07-q1"
  question={<>The scheduler's two main phases are:</>}
  choices={[
    { id: "a", label: "Fetching and binding" },
    { id: "b", label: "Filtering and scoring" },
    { id: "c", label: "Pulling and scheduling" },
    { id: "d", label: "Evicting and placing" },
  ]}
  correctId="b"
  feedback={<p>The scheduler first filters out nodes that can&apos;t host the Pod (resource gaps, taints, affinity violations), then scores the remaining candidates. Highest score wins.</p>}
/>

<Checkpoint id="m07-q2"
  question={<>What's the simplest way to pin a Pod to nodes with label <code>disktype=ssd</code>?</>}
  choices={[
    { id: "a", label: "nodeSelector: disktype: ssd" },
    { id: "b", label: "podAffinity" },
    { id: "c", label: "A NodePort Service" },
    { id: "d", label: "A custom scheduler" },
  ]}
  correctId="a"
  feedback={<p><code>nodeSelector</code> is the simplest. For more expressive matching (soft preferences, multi-valued conditions), use <code>nodeAffinity</code>.</p>}
/>

<Checkpoint id="m07-q3"
  question={<>A node is tainted <code>gpu=true:NoSchedule</code>. Which Pod will land there?</>}
  choices={[
    { id: "a", label: "Any Pod with no nodeSelector" },
    { id: "b", label: "Only Pods with a matching toleration for gpu=true:NoSchedule" },
    { id: "c", label: "Only system Pods in kube-system" },
    { id: "d", label: "No Pod can ever land there" },
  ]}
  correctId="b"
  feedback={<p><code>NoSchedule</code> repels Pods without a matching toleration. Add a toleration to Pods that should run on GPU nodes.</p>}
/>

<Checkpoint id="m07-q4"
  question={<>What are requests used for?</>}
  choices={[
    { id: "a", label: "Setting hard maximums enforced by the kernel" },
    { id: "b", label: "Telling the scheduler the minimum resources to find on a node" },
    { id: "c", label: "Priority during eviction only" },
    { id: "d", label: "Billing metadata" },
  ]}
  correctId="b"
  feedback={<p>Requests are scheduling hints: the scheduler picks a node with at least <code>requests.cpu</code> and <code>requests.memory</code> free. Limits are the kernel-enforced upper bound.</p>}
/>

<Checkpoint id="m07-q5"
  question={<>A container with <code>limits.memory: 256Mi</code> uses 300 MiB. What happens?</>}
  choices={[
    { id: "a", label: "It gets more memory automatically" },
    { id: "b", label: "It's OOMKilled by the kernel" },
    { id: "c", label: "The Pod is rescheduled" },
    { id: "d", label: "Nothing - limits are advisory" },
  ]}
  correctId="b"
  feedback={<p>Memory limits are hard - the kernel kills the container. Pod restart behavior depends on the restart policy. Look for <code>OOMKilled</code> in <code>kubectl describe pod</code>.</p>}
/>

<Checkpoint id="m07-q6"
  question={<>Which QoS class applies when requests = limits for all resources?</>}
  choices={[
    { id: "a", label: "BestEffort" },
    { id: "b", label: "Burstable" },
    { id: "c", label: "Guaranteed" },
    { id: "d", label: "Preferred" },
  ]}
  correctId="c"
  feedback={<p>Guaranteed: every container in the Pod has both requests and limits set for both CPU and memory, and they&apos;re equal. Guaranteed Pods are the last to be evicted under node pressure.</p>}
/>

<Checkpoint id="m07-q7"
  question={<>The Horizontal Pod Autoscaler (HPA) scales:</>}
  choices={[
    { id: "a", label: "Nodes up and down" },
    { id: "b", label: "The number of Pod replicas based on observed metrics" },
    { id: "c", label: "CPU and memory requests on existing Pods" },
    { id: "d", label: "StorageClasses" },
  ]}
  correctId="b"
  feedback={<p>HPA watches a metric (default CPU, but can be custom) and adjusts the target Deployment/ReplicaSet&apos;s replica count. Cluster Autoscaler scales nodes; VPA scales Pod resources.</p>}
/>

<Checkpoint id="m07-q8"
  question={<>Cluster Autoscaler adds a new node when:</>}
  choices={[
    { id: "a", label: "Existing Pods are using more than 50% CPU" },
    { id: "b", label: "Pending Pods can't be scheduled on any existing node due to insufficient resources" },
    { id: "c", label: "A LoadBalancer Service is created" },
    { id: "d", label: "HPA requests it" },
  ]}
  correctId="b"
  feedback={<p>Cluster Autoscaler reacts to <em>pending</em> Pods that no existing node can accommodate - it asks the cloud provider for more capacity. It also scales down underutilized nodes.</p>}
/>

</div>
