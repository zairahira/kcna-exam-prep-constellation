import Checkpoint from "@/components/quiz/Checkpoint";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-accent uppercase tracking-wide mb-1">quiz</p>
  <h1 className="text-2xl font-bold">Cloud Native Architecture</h1>
  <p className="text-fg-subtle text-sm mt-2">8 questions covering all lessons in this module.</p>
</header>

<Checkpoint id="m10-q1"
  question={<>Which Twelve-Factor principle is violated by writing log files to disk inside the container?</>}
  choices={[
    { id: "a", label: "IV. Backing services" },
    { id: "b", label: "VI. Processes" },
    { id: "c", label: "XI. Logs - treat as event streams" },
    { id: "d", label: "VII. Port binding" },
  ]}
  correctId="c"
  feedback={<p>Factor XI: apps should write logs to stdout/stderr and let the platform handle collection and aggregation. Writing to local files ties logs to the container&apos;s filesystem (which disappears).</p>}
/>

<Checkpoint id="m10-q2"
  question={<>Which feature distinguishes serverless from always-on container deployments?</>}
  choices={[
    { id: "a", label: "Serverless always runs on Linux" },
    { id: "b", label: "Scale-to-zero plus event-driven invocation and fine-grained billing" },
    { id: "c", label: "Serverless requires Kubernetes" },
    { id: "d", label: "Serverless uses a proprietary language" },
  ]}
  correctId="b"
  feedback={<p>Scale-to-zero, event-driven, pay-per-invocation billing - these define serverless. Always-on deployments reserve capacity continuously.</p>}
/>

<Checkpoint id="m10-q3"
  question={<>Knative runs on:</>}
  choices={[
    { id: "a", label: "Bare metal only" },
    { id: "b", label: "AWS only" },
    { id: "c", label: "Kubernetes (it extends K8s with serverless semantics)" },
    { id: "d", label: "Docker Swarm" },
  ]}
  correctId="c"
  feedback={<p>Knative is a Kubernetes add-on (Serving + Eventing). It powers Google Cloud Run, IBM Cloud Code Engine, and OpenShift Serverless.</p>}
/>

<Checkpoint id="m10-q4"
  question={<>The CNCF's role is to:</>}
  choices={[
    { id: "a", label: "Build open-source cloud native software" },
    { id: "b", label: "Host, govern, and promote open-source cloud native projects built by the community" },
    { id: "c", label: "Certify commercial cloud providers" },
    { id: "d", label: "Replace the Linux Foundation" },
  ]}
  correctId="b"
  feedback={<p>CNCF is a vendor-neutral home under the Linux Foundation. It hosts projects and provides process + infrastructure; the projects themselves are built by their own communities.</p>}
/>

<Checkpoint id="m10-q5"
  question={<>In order, the CNCF project maturity levels are:</>}
  choices={[
    { id: "a", label: "Beta → GA → LTS" },
    { id: "b", label: "Alpha → Beta → Stable" },
    { id: "c", label: "Sandbox → Incubating → Graduated" },
    { id: "d", label: "Experimental → Supported → Deprecated" },
  ]}
  correctId="c"
  feedback={<p>Sandbox (early), Incubating (active), Graduated (mature, widely adopted). Moving up requires a TOC vote.</p>}
/>

<Checkpoint id="m10-q6"
  question={<>Which is a Graduated CNCF project?</>}
  choices={[
    { id: "a", label: "OpenTelemetry" },
    { id: "b", label: "Backstage" },
    { id: "c", label: "Prometheus" },
    { id: "d", label: "Crossplane" },
  ]}
  correctId="c"
  feedback={<p>Prometheus is Graduated (one of the earliest after Kubernetes). OpenTelemetry, Backstage, and Crossplane are all Incubating.</p>}
/>

<Checkpoint id="m10-q7"
  question={<>What's the best one-liner for DevOps?</>}
  choices={[
    { id: "a", label: "A specific job title reserved for senior engineers" },
    { id: "b", label: "A set of Kubernetes tools" },
    { id: "c", label: "A culture emphasising shared responsibility between dev and ops, automation, and fast feedback" },
    { id: "d", label: "An AWS service" },
  ]}
  correctId="c"
  feedback={<p>DevOps is primarily cultural. "DevOps Engineer" as a title exists but usually refers to platform/SRE-style work. The principles (shared responsibility, automation, blameless postmortems) matter more than the title.</p>}
/>

<Checkpoint id="m10-q8"
  question={<>Platform engineering's core idea is:</>}
  choices={[
    { id: "a", label: "Every developer manages their own Kubernetes cluster" },
    { id: "b", label: "Build an internal developer platform as a product; developers consume it self-service" },
    { id: "c", label: "Outsource operations to a cloud provider" },
    { id: "d", label: "Eliminate developers entirely" },
  ]}
  correctId="b"
  feedback={<p>Platform engineering treats the internal developer platform as a product with users (developers). The team encodes paved paths, opinions, and defaults so developers get productive quickly without learning every detail of the underlying infrastructure.</p>}
/>

</div>
