import Remember from "@/components/Remember";

<div className="max-w-3xl mx-auto px-8 py-10">

<header className="cc-card p-5 mb-10">
  <p className="mono text-xs text-fg-subtle uppercase tracking-wide mb-1">review</p>
  <h1 className="text-2xl font-bold">Cloud Native Architecture</h1>
  <p className="text-fg-subtle text-sm mt-2">Key facts from all lessons in this module.</p>
</header>

<Remember
  points={[
    <>The <strong>Twelve-Factor App</strong> methodology predates cloud native but is the blueprint cloud native platforms assume.</>,
    <>Key factors to remember: <strong>III Config</strong> (in the environment), <strong>VI Processes</strong> (stateless), <strong>IX Disposability</strong> (fast startup, graceful shutdown), <strong>XI Logs</strong> (as streams to stdout).</>,
    <>Kubernetes directly supports most factors. Apps that violate 12-factor heavily fight the platform.</>,
  ]}
/>

<Remember
  points={[
    <><strong>Serverless</strong> = no server management, scale-to-zero, event-driven, fine-grained billing.</>,
    <><strong>FaaS</strong> (Functions-as-a-Service): deploy a function. Examples: AWS Lambda, Google Cloud Functions, Azure Functions, OpenFaaS.</>,
    <><strong>Serverless containers</strong>: deploy a container image. Examples: Google Cloud Run, AWS Fargate, Knative.</>,
    <><strong>Knative</strong> (CNCF Incubating) = serverless on Kubernetes. Two parts: Serving (HTTP) and Eventing (events).</>,
    <>Cold starts and execution limits are the typical trade-offs.</>,
  ]}
/>

<Remember
  points={[
    <>The <strong>CNCF</strong> hosts projects; it doesn&apos;t build them. Sits under the Linux Foundation.</>,
    <>Governance: <strong>TOC</strong> (technical oversight), <strong>SIGs</strong> (per topic), <strong>Working Groups</strong>.</>,
    <>Maturity levels: <strong>Sandbox</strong> → <strong>Incubating</strong> → <strong>Graduated</strong>. Level = a signal, not a guarantee.</>,
    <><strong>Landscape</strong> (<code>landscape.cncf.io</code>) = map of the whole ecosystem. <strong>Trail Map</strong> = suggested learning/adoption path.</>,
  ]}
/>

<Remember
  points={[
    <>Roles: <strong>Developer</strong> (writes app), <strong>Platform Engineer / SRE</strong> (runs platform), <strong>Security Engineer</strong>, <strong>Architect</strong>. Heavy overlap in practice.</>,
    <><strong>DevOps</strong> is a culture (shared responsibility, automation, fast feedback), not strictly a job title.</>,
    <><strong>Platform engineering</strong> = platform team builds internal developer platform as a product; devs consume it self-service.</>,
    <>Tools: <strong>Backstage</strong> (developer portal, CNCF Incubating), <strong>Crossplane</strong> (platform building blocks, CNCF Incubating).</>,
  ]}
/>

</div>
